const Discord = require('discord.js')
var botprefix = ('f/')
module.exports = {
    name: "{command name}",
    description: "{command description}",
    async run (bot, message, args) {
        //command stuff here
    }
}
