const Discord = require('discord.js')
module.exports = {
    name: "console",
    description: "ping command",
    async run (bot, message, args) {
        console.log("fix your vunerabilities")
    }
}